<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center"
    android:background= "@drawable/bg_edittext_rounded">
    <!-- Icon -->
    <ImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@drawable/faceid2"
        android:layout_marginBottom="16dp"/>

    <!-- Tiêu đề -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CĂN CHỈNH HƯỚNG LÁI"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#00BCD4" />

    <!-- Mô tả hướng dẫn -->
    <TextView
        android:id="@+id/tvCalibrationDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Đặt điện thoại cố định trên táp lô hoặc góc bất kỳ, ngồi đúng tư thế lái xe, nhìn thẳng về phía trước rồi nhấn &quot;Bắt đầu ngay&quot;."
        android:textAlignment="center"
        android:layout_marginTop="12dp"
        android:textSize="15sp"
        android:textColor="#333333" />

    <!-- Số đếm ngược lớn -->
    <TextView
        android:id="@+id/tvCountdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="5"
        android:textSize="64sp"
        android:textStyle="bold"
        android:textColor="#FF5722"
        android:visibility="gone"
        android:layout_marginTop="16dp" />

    <!-- Progress bar trong khi đếm -->
    <ProgressBar
        android:id="@+id/pbCalibration"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:indeterminate="true"
        android:visibility="invisible" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStartCalibration"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Bắt đầu ngay"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="#00BCD4"
        app:cornerRadius="30dp" />

</LinearLayout>